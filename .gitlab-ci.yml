image:
  name: registry.gitlab.com/partisia/dockerimages/mkdocs:latest
variables:
  GIT_SUBMODULE_STRATEGY: recursive

stages:
- prepare
- build
- postprocess
- deploy

prepare-zip:
  image: registry.gitlab.com/partisia/dockerimages/rust:latest
  stage: prepare
  only:
    refs:
    - master
    changes:
    - version.txt
  script:
  - ./build-zip.sh

build-site:
  stage: build
  script:
  - mkdocs build --site-dir public
  artifacts:
    paths:
    - public

pages:
  image: registry.gitlab.com/partisia/dockerimages/rust:latest
  stage: deploy
  dependencies:
  - build-site
  script:
  - ./replace-zip-version.sh
  only:
  - master
  artifacts:
    paths:
    - public

pages-dev:
  image: registry.gitlab.com/partisia/dockerimages/rust:latest
  stage: deploy
  dependencies:
  - build-site
  script:
  - ./replace-zip-version.sh
  except:
  - master
  artifacts:
    paths:
    - public
